rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write for Jelajah CBP Rupiah data
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
